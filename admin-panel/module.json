{
  "name": "admin-panel",
  "version": "0.1.0",
  "description": "Admin panel for generating and managing invite codes (used at registration)",
  "category": "auth",
  "framework": { "next": ">=14", "react": ">=18" },
  "dependencies": {
    "npm": ["better-sqlite3@^9.6.0"],
    "modules": ["authentication"]
  },
  "files": [
    { "from": "api/admin/codes/route.ts", "to": "src/app/api/admin/codes/route.ts" },
    { "from": "pages/admin/page.tsx", "to": "src/app/admin/page.tsx", "optional": true }
  ],
  "envVars": [],
  "requiresContract": {
    "tables": ["users (with is_admin column)", "invite_codes"]
  },
  "postInstall": [
    "Nothing extra — the auth module's schema is sufficient",
    "Promote a user to admin: UPDATE users SET is_admin = 1 WHERE username = 'X'",
    "Visit /admin to generate codes"
  ]
}
